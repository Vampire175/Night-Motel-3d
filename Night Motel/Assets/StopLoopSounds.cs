using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StopLoopSounds : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SoundManager.StopLoopSound();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
